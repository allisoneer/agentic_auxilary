[workspace]

[package]
name = "example-03-tool-calling"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
anthropic-async = { path = "../..", features = ["schemars"] }
tokio = { version = "1", features = ["full"] }
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
schemars = { version = "1", features = ["derive"] }
