[workspace]

[package]
name = "multimodal"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
anthropic-async = { path = "../.." }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
anyhow = "1"
